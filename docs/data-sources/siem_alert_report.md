---
page_title: "rapid7_siem_alert_report"
subcategory: ""
description: |-
  
---

# rapid7_siem_alert_report (Data Source)





## Example

```terraform
data "rapid7_siem_alert_report" "report" {
  from = timeadd(plantimestamp(), "-${30 * 24}h") # 30 days ago
  to   = plantimestamp()                          # Now

  # Filter by status
  query = "alert.status IN [OPEN, INVESTIGATING, CLOSED]"

  aggregation = [
    ["alert.organization.id", "alert.organization.name"],
    # Aggregate by rule
    ["alert.rule.rrn", "alert.rule.name"],
    # Then by status
    ["alert.status"]
  ]
}
```



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `aggregation` (List of List of String) Levels of aggregation used to generate the report. Each level of aggregation specifies one or more fields to group by.
For example, to aggregate alerts by rule, then by their status:
```
aggregation = [
  [
    "alert.rule.rrn",
    "alert.rule.name"
  ],
  [
    "alert.status"
  ]
]
```
- `from` (String) RFC3339 timestamp of the oldest alert included in the report
- `to` (String) RFC3339 timestamp of the newest alert included in the report

### Optional

- `query` (String) LEQL query to filter alerts aggregated by the report
- `results` (Attributes List) Aggregated statistics for each set of unique values of the fields specified in the corresponding aggregation level (see [below for nested schema](#nestedatt--results))

<a id="nestedatt--results"></a>
### Nested Schema for `results`

Required:

- `count` (Number) Total count of results having the field values described by `fields` and any subsequent levels of aggregation
- `fields` (Map of String) A map of field names and values which all alerts at this level of aggregation have in common

Optional:

- `results` (Attributes List) Aggregated statistics for each set of unique values of the fields specified in the corresponding aggregation level (see [below for nested schema](#nestedatt--results--results))

<a id="nestedatt--results--results"></a>
### Nested Schema for `results.results`

Required:

- `count` (Number) Total count of results having the field values described by `fields` and any subsequent levels of aggregation
- `fields` (Map of String) A map of field names and values which all alerts at this level of aggregation have in common

Optional:

- `results` (Attributes List) Aggregated statistics for each set of unique values of the fields specified in the corresponding aggregation level (see [below for nested schema](#nestedatt--results--results--results))

<a id="nestedatt--results--results--results"></a>
### Nested Schema for `results.results.results`

Required:

- `count` (Number) Total count of results having the field values described by `fields` and any subsequent levels of aggregation
- `fields` (Map of String) A map of field names and values which all alerts at this level of aggregation have in common

Optional:

- `results` (Attributes List) Aggregated statistics for each set of unique values of the fields specified in the corresponding aggregation level (see [below for nested schema](#nestedatt--results--results--results--results))

<a id="nestedatt--results--results--results--results"></a>
### Nested Schema for `results.results.results.results`

Required:

- `count` (Number) Total count of results having the field values described by `fields` and any subsequent levels of aggregation
- `fields` (Map of String) A map of field names and values which all alerts at this level of aggregation have in common

Optional:

- `results` (Attributes List) Aggregated statistics for each set of unique values of the fields specified in the corresponding aggregation level (see [below for nested schema](#nestedatt--results--results--results--results--results))

<a id="nestedatt--results--results--results--results--results"></a>
### Nested Schema for `results.results.results.results.results`

Required:

- `count` (Number) Total count of results having the field values described by `fields` and any subsequent levels of aggregation
- `fields` (Map of String) A map of field names and values which all alerts at this level of aggregation have in common

Optional:

- `results` (Attributes List) Aggregated statistics for each set of unique values of the fields specified in the corresponding aggregation level (see [below for nested schema](#nestedatt--results--results--results--results--results--results))

<a id="nestedatt--results--results--results--results--results--results"></a>
### Nested Schema for `results.results.results.results.results.results`

Required:

- `count` (Number) Total count of results having the field values described by `fields` and any subsequent levels of aggregation
- `fields` (Map of String) A map of field names and values which all alerts at this level of aggregation have in common
